# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  activerecord:
    errors:
      models:
        contact_request:
          attributes:
            body:
              blank: "Please enter a question or comment"
            email:
              blank: "We require a valid email address in order to respond"
              invalid: "We require a valid email address in order to respond"
    attributes:
      contact_request:
        body: ""

